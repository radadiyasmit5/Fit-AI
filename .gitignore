# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native builds (generated — run `expo run:ios` / `expo run:android` to recreate)
/ios
/android
.kotlin/

# Native signing & certificates — never commit these
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.cer
*.certSigningRequest
*.provisionprofile

# Metro bundler
.metro-health-check*

# Debug logs
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment variables — commit .env.example, never .env
.env
.env.local
.env.*.local
.env.development
.env.production

# TypeScript
*.tsbuildinfo

# Editor
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo

# Supabase local dev (keep migrations, ignore local config)
supabase/.branches
supabase/.temp

# Build output
build/
out/
.vscode